# 제품 요구사항 정의서 (PRD)

## 1. 개요 (Overview)
- **제품명**: AI 모델학습(TM) 제미나이 캔버스 웹 앱 (Teachable Machine & Web BLE Controller)
- **목적**: 웹캠 비디오 스트림을 활용하여 브라우저 환경에서 티처블 머신(Teachable Machine) 이미지 분류 모델을 실행하고, AI 추론 결과에 매핑된 커스텀 제어 문자를 Web Bluetooth API를 통해 외부 ESP32 마이크로컨트롤러로 실시간 전송하여 하드웨어(네오픽셀 LED 등)를 제어합니다.
- **주요 특징**:
  - 단일 웹 문서(HTML/CSS/JS) 구조로 제미나이 캔버스 등 샌드박스 환경에서 즉시 동작.
  - HSL 다크 테마 및 Glassmorphism 반응형 레이아웃 탑재.
  - ESP32 MicroPython 수신부 펌웨어 동작 스펙에 맞춘 문자열 치환 매핑 테이블 지원.
  - 보안 환경 제약 충족을 위한 스캔 디바이스 필터링 및 HTTPS 보안 권장 경고 제공.

---

## 2. 주요 기능 및 상세 기술 요구사항

### 2.1. 웹캠 비디오 및 캔버스 제어 (p5.js v1.9.0)
- **정방형 비디오 렌더링**:
  - 사용자의 카메라 입력을 해상도 640x480으로 캡처하고, 중앙 영역을 400x400 정사각형으로 크롭하여 1:1 비율 캔버스에 출력합니다.
- **좌우 반전 (거울 모드)**:
  - 전면 카메라 사용 시 시각적 일관성을 확보하기 위해 수평 좌표축을 반전(`scale(-1, 1)`)하는 토글 기능을 제공합니다.
- **카메라 디바이스 제어 및 전후방 스위칭**:
  - `navigator.mediaDevices.enumerateDevices`를 통해 사용자 시스템의 사용 가능한 모든 비디오 입력 장치를 검색하여 선택 드롭다운을 구성합니다.
  - 모바일 및 태블릿 등 멀티 카메라 환경에서 `facingMode`를 `user`(전면) 또는 `environment`(후면)로 명시적 전환하며 캡처 채널을 재시작하는 토글 버튼을 탑재합니다.

### 2.2. Teachable Machine AI 모델 로드 및 추론 (ml5.js v0.12.2)
- **동적 로드 기능**:
  - 사용자가 모델 전체 공유 URL(예: `https://teachablemachine.withgoogle.com/models/xxxxxx/`) 또는 고유 짧은 ID만 입력해도 도메인과 `model.json` 주소를 정규화하여 가중치 파일을 안전하게 호출합니다.
- **실시간 추론 및 시각화**:
  - ml5.js의 `imageClassifier` 추론 엔진을 호출하여 캡처 비디오 스트림 프레임을 실시간으로 판정합니다.
  - 최상위 정확도 3개 클래스 라벨 및 신뢰도를 나타내는 가로 바(Bar) 차트를 실시간으로 동기화합니다.
  - 최우선으로 검출된 사물 클래스명과 신뢰도 백분율(%) 데이터를 p5.js 캔버스 영역 하단에 반투명 검은색 오버레이 레이어로 렌더링합니다.

### 2.3. 클래스별 송신값 매핑(Mapping) 테이블
- **동적 클래스 감지**:
  - 모델 로딩이 완료되면 내장된 사물 분류 라벨 리스트를 자동으로 파싱하여 동적으로 매핑 테이블 UI를 렌더링합니다.
- **사용자 지정 제어 문자 설정**:
  - 감지된 사물 라벨별로 ESP32 마이크로컨트롤러에 전송할 제어 명령 문자열(예: `'1'`, `'2'`, `'3'`, `'4'`)을 지정할 수 있는 입력 필드를 제공합니다.
  - 입력된 매핑 맵 객체는 로컬 스토리지(`tm_class_map`)에 저장되어 페이지 새로고침 시에도 유지됩니다.
  - 기본값은 MicroPython 스펙에 맞추어 `1`, `2`, `3`, `4` 순서대로 순차 지정됩니다.

### 2.4. ESP32 Bluetooth (Web Bluetooth API) 연동 및 전송 제어 (가이드 최적화)

제미나이 캔버스(AI)가 자바스크립트로 구현 시 헷갈리지 않도록 Bluetooth 연결 및 데이터 채널의 기술 사양을 구체적으로 정의합니다.

#### A. 장치 스캔 및 페어링 필터링
- Web Bluetooth API `navigator.bluetooth.requestDevice` 호출 시, 주변의 무관한 장치를 걸러내고 타깃 장치만 띄우기 위해 다음과 같은 필터를 지정합니다:
  ```javascript
  {
    filters: [
      {
        services: ['6e400001-b5a3-f393-e0a9-e50e24dcca9e'], // UART Service UUID 
        namePrefix: 'ESP_' // 기기 이름이 'ESP_'로 시작하는 장치만 스캔 목록에 노출
      }
    ]
  }
  ```

#### B. 데이터 통신 채널 UUID 및 연동 인터페이스 사양
웹 브라우저와 ESP32 간의 데이터 교환은 Nordic UART Service (NUS) 표준 규격을 따릅니다.

| 채널 이름 (Web 관점) | Characteristic UUID | 지원 속성 (Properties) | 자바스크립트 구현 방식 |
| :--- | :--- | :--- | :--- |
| **송신 채널 (TX)** | `6e400002-b5a3-f393-e0a9-e50e24dcca9e` | **Write** 또는 **Write Without Response** | 브라우저에서 ESP32로 명령 바이트 전송 시 `writeValueWithoutResponse(buffer)`를 사용하여 지연 없이 발송 처리함. |
| **수신 채널 (RX)** | `6e400003-b5a3-f393-e0a9-e50e24dcca9e` | **Notify** | ESP32로부터 데이터를 수신하기 위해 `startNotifications()`를 호출하고, `characteristicvaluechanged` 이벤트 리스너를 바인딩하여 수신 버퍼 디코딩 처리함. |

#### C. 실시간 데이터 전송 최적화 필터
- **비동기 송신 보호 (`isSendingData`)**:
  - BLE의 물리적 전송 속도 제한으로 패킷이 겹치면 전송 실패 에러가 발생합니다. 데이터를 쓰는 비동기 함수 진입 시 `isSendingData = true` 락(Lock)을 걸어 전송 중 추가 송신 시도를 무시 처리하고, `finally` 블록에서 락을 해제합니다.
- **스로틀링 (Throttle)**:
  - 사용자가 설정한 최소 전송 주기(50ms ~ 2000ms, 기본값 300ms) 슬라이더 값에 기초하여, `Date.now() - lastSentTime`이 주기 미만일 경우 전송을 스킵합니다.
- **중복 전송 방지**:
  - 직전에 전송한 결과와 동일한 데이터는 중복 필터 스위치가 켜져 있을 경우 전송을 제한합니다.
- **구분자 설정 (Delimiter)**:
  - 수신 측 MicroPython의 문자열 트리밍(`strip()`) 조건에 맞춰 문자열 종단에 LF(`\n`), CR+LF(`\r\n`), 또는 구분자 없음 옵션을 지원합니다.

### 2.5. MicroPython 펌웨어 연동 가이드 패널
- **인-앱 펌웨어 가이드**:
  - Thonny 환경에서 활용할 수 있는 MicroPython 연동 전체 예제 코드를 내장합니다.
  - RGB LED(Red=GPIO 25, Green=GPIO 26, Blue=GPIO 27)가 브라우저에서 전송받은 데이터 `'1'`, `'2'`, `'3'`에 반응하여 제어되는 전체 제어 블록을 가이드 형태로 제공합니다.
  - 간편한 **"코드 복사"** 버튼을 제공하여 교육용 사용성과 사용자 경험을 향상시킵니다.

---

## 3. UI/UX 및 디자인 에스테틱 요구사항

### 3.1. 레이아웃 및 반응형 디자인 (Responsive Layout)
- **데스크톱 (min-width: 768px)**:
  - 2열 레이아웃으로, 좌측 카메라는 화면 상단에 Sticky로 밀착 고정되며, 우측의 컨트롤 패널 영역만 독립적으로 스크롤되도록 설계합니다.
- **모바일 세로 (Mobile Portrait)**:
  - 상단에 캔버스를 Sticky 형태로 화면 상단에 강제 고정하여 카메라를 보면서 아래로 스크롤하여 제어 요소를 튜닝할 수 있도록 설정합니다.
- **모바일 가로 (Mobile Landscape - max-height: 550px)**:
  - 화면 높이를 절약하기 위해 좌우 2열 배치(캔버스 45%, 제어 패널 50%)로 강제 전환되며 푸터 등 불필요한 장식 요소를 자동 숨김 처리합니다.

### 3.2. 상태 표시 영역 피드백
- **블루투스 상태 배지**: '연결 대기 중'(분홍 배지), '연결 스캔 중'(황색 배지), '연결됨'(녹색 배지) 등 상태 변화에 따른 색상 및 텍스트 전환.
- **모델 상태 배지**: '준비 대기 중', '다운로드 중'(깜박이는 황색 도트), '정상 로드 완료'(녹색 도트 및 활성화 배지) 등 단계적 피드백 제공.
- **통신 모니터 로그**: 실시간으로 발송(TX), 수신(RX) 및 시스템 에러 내역을 타임스탬프와 함께 직관적인 색깔로 터미널형 박스에 출력합니다.
- **Pretendard 웹 폰트**와 다크 모드 테마를 채택하여 세련된 디자인 아이덴티티를 확립합니다.
